
// This file is moved to src/app/[username]/sw.js/route.ts to support dynamic tenant subdomains.
// It can be safely removed or kept as a placeholder.
export async function GET() {
    return new Response('Moved to dynamic tenant route', { status: 404 });
}
